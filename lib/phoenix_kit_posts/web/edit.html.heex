  <div class="p-6">
    <%!-- Header Section --%>
    <.admin_page_header back={Routes.path("/admin/posts")} />

    <.form
      for={@form}
      id="post-form"
      phx-change="validate"
      phx-submit="save"
    >
      <%!-- Main Layout: 2/3 Content + 1/3 Sidebar --%>
      <%!-- Using CSS grid (3 cols, content spans 2) instead of flex because --%>
      <%!-- flex's grow/shrink algorithm doesn't reliably honor the 2:1 ratio --%>
      <%!-- when a child (the leaf editor) has a large intrinsic min-content --%>
      <%!-- width — even with min-w-0 on every item, flex was collapsing the --%>
      <%!-- sidebar to 0 after the editor mounted. Grid distributes space --%>
      <%!-- rigidly and accounts for the gap automatically. --%>
      <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
        <%!-- Left Column: Main Content (2/3) --%>
        <%!-- min-w-0 keeps grid items from being inflated by intrinsic content. --%>
        <%!-- overflow-hidden clips anything that would still try to bust out. --%>
        <div class="lg:col-span-2 min-w-0 overflow-hidden flex flex-col gap-6">
          <%!-- Basic Information Card --%>
          <div class="card bg-base-100 shadow-xl">
            <div class="card-body">
              <h2 class="card-title text-xl mb-4">
                <.icon name="hero-document-text" class="w-5 h-5" /> Basic Information
              </h2>

              <div class="space-y-4">
                <%!-- Title --%>
                <div class="fieldset">
                  <label class="label">
                    <span class="fieldset-legend font-medium">Title *</span>
                    <span class="fieldset-label">
                      {String.length(@form[:title].value || "")}/{@max_title_length}
                    </span>
                  </label>
                  <.input
                    field={@form[:title]}
                    type="text"
                    placeholder="Enter post title..."
                    maxlength={@max_title_length}
                    required
                  />
                </div>

                <%!-- Subtitle --%>
                <div class="fieldset">
                  <label class="label">
                    <span class="fieldset-legend font-medium">Subtitle / Tagline</span>
                    <span class="fieldset-label">
                      {String.length(@form[:sub_title].value || "")}/{@max_subtitle_length}
                    </span>
                  </label>
                  <.input
                    field={@form[:sub_title]}
                    type="text"
                    placeholder="Optional subtitle or tagline..."
                    maxlength={@max_subtitle_length}
                  />
                </div>

                <%!-- Media — delegated to the canonical PhoenixKit gallery.
                     Picker + drag-reorder + featured badge + lightbox are all
                     handled inside the component; the parent only reacts to the
                     {MediaGallery, id, {:changed, uuids}} message it emits. --%>
                <div class="fieldset">
                  <label class="label">
                    <span class="fieldset-legend font-medium">Media</span>
                    <span class="fieldset-label">{length(@post_images)}</span>
                  </label>

                  <.live_component
                    module={PhoenixKitWeb.Components.MediaGallery}
                    id="post-images-gallery"
                    selected={Enum.map(@post_images, &to_string(&1.file_uuid))}
                    mode={:multiple}
                    cols={4}
                    featured_first={true}
                    max_count={@max_media}
                    phoenix_kit_current_user={assigns[:phoenix_kit_current_user]}
                  />

                  <label class="label">
                    <span class="fieldset-label text-base-content/70">
                      Drag to reorder. First image is the featured image.
                    </span>
                  </label>
                </div>

                <%!-- Content --%>
                <div class="fieldset">
                  <label class="label">
                    <span class="fieldset-legend font-medium">Content *</span>
                    <span class="fieldset-label">
                      {String.length(@content)}/{@max_content_length}
                    </span>
                  </label>
                  <.leaf_editor
                    id="post-content-editor"
                    content={@content}
                    mode={@editor_mode}
                    placeholder="Write your post content here... Use #hashtags for auto-tagging"
                    height="400px"
                    debounce={400}
                    toolbar={[:image, :video]}
                    wiki_links={%{resolve: true}}
                  />
                  <label class="label">
                    <span class="fieldset-label text-base-content/70">
                      Use #hashtags in your content for automatic tag extraction
                    </span>
                  </label>
                </div>

                <%!-- SEO Slug --%>
                <div class="fieldset">
                  <label class="label">
                    <span class="fieldset-legend font-medium">SEO Slug</span>
                  </label>
                  <.input
                    field={@form[:slug]}
                    type="text"
                    placeholder={
                      if @seo_auto_slug,
                        do: "Auto-generated from title...",
                        else: "enter-url-slug"
                    }
                    disabled={@seo_auto_slug}
                  />
                  <label class="label">
                    <span class="fieldset-label text-base-content/70">
                      <%= if @seo_auto_slug do %>
                        Auto-generation enabled. URL slug will be created from title.
                      <% else %>
                        URL-friendly identifier (e.g., my-awesome-post)
                      <% end %>
                    </span>
                  </label>
                </div>
              </div>
            </div>
          </div>

          <%!-- Groups Card (if enabled) --%>
          <%= if @allow_groups do %>
            <div class="card bg-base-100 shadow-xl">
              <div class="card-body">
                <h2 class="card-title text-xl mb-4">
                  <.icon name="hero-folder" class="w-5 h-5" /> Collections / Groups
                </h2>

                <%= if length(@user_groups) > 0 do %>
                  <div class="fieldset">
                    <div class="space-y-2">
                      <%= for group <- @user_groups do %>
                        <label class="label cursor-pointer justify-start gap-3">
                          <input
                            type="checkbox"
                            name="post[group_ids][]"
                            value={group.uuid}
                            checked={group.uuid in @selected_groups}
                            class="checkbox checkbox-primary"
                          />
                          <span class="fieldset-legend">
                            <span class="font-semibold">{group.name}</span>
                            <%= if group.description do %>
                              <span class="text-sm text-base-content/60">
                                - {group.description}
                              </span>
                            <% end %>
                          </span>
                        </label>
                      <% end %>
                    </div>
                    <label class="label">
                      <span class="fieldset-label text-base-content/70">
                        Organize posts into your personal collections
                      </span>
                    </label>
                  </div>
                <% else %>
                  <div class="text-sm text-base-content/60">
                    No groups created yet.
                    <.link navigate={Routes.path("/admin/posts/groups")} class="link link-primary">
                      Create your first group
                    </.link>
                    to organize your posts.
                  </div>
                <% end %>
              </div>
            </div>
          <% end %>

          <%!-- Tags Card --%>
          <div class="card bg-base-100 shadow-xl">
            <div class="card-body">
              <h2 class="card-title text-xl mb-4">
                <.icon name="hero-hashtag" class="w-5 h-5" /> Tags
              </h2>

              <%!-- Selected Tags Display --%>
              <%= if length(@selected_tags) > 0 do %>
                <div class="flex flex-wrap gap-2 mb-4">
                  <%= for tag <- @selected_tags do %>
                    <div class="badge badge-primary gap-2">
                      #{tag}
                      <button
                        type="button"
                        phx-click="remove_tag"
                        phx-value-tag={tag}
                        class="btn btn-ghost btn-xs p-0"
                      >
                        ✕
                      </button>
                    </div>
                  <% end %>
                </div>
              <% else %>
                <div class="text-sm text-base-content/60 mb-4">
                  No tags added yet. Tags will be automatically extracted from #hashtags in your content.
                </div>
              <% end %>

              <%!-- Manual Tag Input --%>
              <div class="fieldset">
                <label class="label">
                  <span class="fieldset-legend font-medium">Add Tag Manually</span>
                  <span class="fieldset-label">
                    {length(@selected_tags)}/{@max_tags} tags
                  </span>
                </label>
                <div class="input-group">
                  <span class="bg-base-200 px-4 py-3">#</span>
                  <input
                    type="text"
                    id="tag-input"
                    placeholder="Enter tag and press Enter"
                    class="input w-full"
                    phx-keydown="add_tag"
                    phx-key="Enter"
                  />
                </div>
                <label class="label">
                  <span class="fieldset-label text-base-content/70">
                    Tags are automatically extracted from #hashtags in content
                  </span>
                </label>
              </div>
            </div>
          </div>
        </div>

        <%!-- Right Column: Sidebar (1/3) --%>
        <div class="min-w-0 flex flex-col gap-6 lg:sticky lg:top-6 lg:self-start">
          <%!-- Post Info Card (only for existing posts) --%>
          <%= if @post.uuid do %>
            <div class="card bg-base-100 shadow-xl">
              <div class="card-body">
                <h2 class="card-title text-xl mb-4">
                  <.icon name="hero-information-circle" class="w-5 h-5" /> Post Info
                </h2>

                <div class="space-y-3">
                  <%!-- Created By --%>
                  <div class="flex items-center gap-2 text-sm">
                    <.icon name="hero-user-circle" class="w-4 h-4 text-base-content/50" />
                    <span class="text-base-content/70">Created by:</span>
                    <span class="font-medium">
                      <%= if @post.user do %>
                        {@post.user.email}
                      <% else %>
                        Unknown
                      <% end %>
                    </span>
                  </div>

                  <%!-- Created When --%>
                  <div class="flex items-center gap-2 text-sm">
                    <.icon name="hero-calendar" class="w-4 h-4 text-base-content/50" />
                    <span class="text-base-content/70">Created:</span>
                    <span class="font-medium">
                      {Calendar.strftime(@post.inserted_at, "%B %d, %Y at %H:%M")}
                    </span>
                  </div>

                  <%!-- Public Link (only for public posts with a slug) --%>
                  <%= if @post.status == "public" && @post.slug && @post.slug != "" do %>
                    <div class="flex items-start gap-2 text-sm">
                      <.icon name="hero-link" class="w-4 h-4 text-base-content/50 mt-0.5" />
                      <div class="flex-1 min-w-0">
                        <span class="text-base-content/70">Public URL:</span>
                        <a
                          href={"/posts/#{@post.slug}"}
                          target="_blank"
                          rel="noopener"
                          class="block text-primary hover:underline truncate font-mono text-xs mt-1"
                          title={"/posts/#{@post.slug}"}
                        >
                          /posts/{@post.slug}
                        </a>
                      </div>
                    </div>
                  <% else %>
                    <div class="flex items-center gap-2 text-sm text-base-content/50">
                      <.icon name="hero-link" class="w-4 h-4" />
                      <span class="italic">Publish to get a public URL</span>
                    </div>
                  <% end %>
                </div>
              </div>
            </div>
          <% end %>

          <%!-- Post Settings Card --%>
          <div class="card bg-base-100 shadow-xl">
            <div class="card-body">
              <h2 class="card-title text-xl mb-4">
                <.icon name="hero-cog-6-tooth" class="w-5 h-5" /> Post Settings
              </h2>

              <div class="space-y-4">
                <%!-- Type --%>
                <div class="fieldset">
                  <label class="label">
                    <span class="fieldset-legend font-medium">Post Type</span>
                  </label>
                  <.select
                    field={@form[:type]}
                    options={[
                      {"Post (Full content)", "post"},
                      {"Snippet (Short update)", "snippet"},
                      {"Repost (Share existing)", "repost"}
                    ]}
                  />
                  <label class="label">
                    <span class="fieldset-label text-base-content/70">
                      Different types have different display layouts
                    </span>
                  </label>
                </div>

                <%!-- Status --%>
                <div class="fieldset">
                  <label class="label">
                    <span class="fieldset-legend font-medium">Status</span>
                  </label>
                  <.select
                    field={@form[:status]}
                    options={[
                      {"Draft", "draft"},
                      {"Public", "public"},
                      {"Unlisted", "unlisted"},
                      {"Scheduled", "scheduled"}
                    ]}
                  />
                  <label class="label">
                    <span class="fieldset-label text-base-content/70">
                      Control post visibility
                    </span>
                  </label>
                </div>

                <%!-- Scheduled Date (only show if status is scheduled) --%>
                <%= if @allow_scheduling and @form[:status].value == "scheduled" do %>
                  <div class="fieldset">
                    <label class="label">
                      <span class="fieldset-legend font-medium">Scheduled Publish Time</span>
                    </label>
                    <.input
                      field={@form[:scheduled_at]}
                      type="datetime-local"
                      required={@form[:status].value == "scheduled"}
                    />
                    <label class="label">
                      <span class="fieldset-label text-base-content/70">
                        Post will be published at this time
                      </span>
                    </label>
                  </div>
                <% end %>
              </div>
            </div>
          </div>

          <%!-- Action Buttons Card --%>
          <div class="card bg-base-100 shadow-xl">
            <div class="card-body">
              <div class="flex flex-col gap-3">
                <button
                  type="submit"
                  class="btn btn-primary w-full"
                  phx-disable-with="Saving..."
                >
                  <.icon name="hero-check" class="w-4 h-4" />
                  {if @post.uuid, do: "Update Post", else: "Create Post"}
                </button>

                <button
                  type="button"
                  phx-click="cancel"
                  class="btn btn-ghost btn-outline w-full"
                >
                  <.icon name="hero-x-mark" class="w-4 h-4" /> Cancel
                </button>
              </div>
            </div>
          </div>
        </div>
      </div>
    </.form>
  </div>

  <%!-- Media Selector Modal --%>
  <.live_component
    :if={@show_media_selector}
    module={PhoenixKitWeb.Live.Components.MediaSelectorModal}
    id="post-media-selector"
    show={@show_media_selector}
    mode={@media_selector_mode}
    selected_uuids={[]}
    phoenix_kit_current_user={assigns[:phoenix_kit_current_user]}
  />

  <script>
    // Function to insert media into Leaf editor at cursor position
    window.postsEditorInsertMedia = function(fileUrl, mediaType) {
      // Try visual mode first (contenteditable div)
      const visualEl = document.getElementById('post-content-editor-visual');
      if (visualEl) {
        visualEl.focus();
        const img = document.createElement('img');
        img.src = fileUrl;
        img.alt = mediaType === 'image' ? 'Image description' : 'Video';
        const sel = window.getSelection();
        if (sel.rangeCount) {
          const range = sel.getRangeAt(0);
          range.deleteContents();
          range.insertNode(img);
          range.collapse(false);
        } else {
          visualEl.appendChild(img);
        }
        // Trigger input event so Leaf picks up the change
        visualEl.dispatchEvent(new Event('input', { bubbles: true }));
        return;
      }

      // Markdown mode fallback
      const textarea = document.getElementById('post-content-editor-markdown-textarea');
      if (!textarea) return;

      const altText = mediaType === 'image' ? 'Image description' : 'Video';
      const template = '![' + altText + '](' + fileUrl + ')';

      const start = textarea.selectionStart || 0;
      const end = textarea.selectionEnd || 0;
      const currentValue = textarea.value;

      const newValue = currentValue.substring(0, start) + template + currentValue.substring(end);
      textarea.value = newValue;

      const newPos = start + template.length;
      textarea.selectionStart = textarea.selectionEnd = newPos;
      textarea.focus();

      textarea.dispatchEvent(new Event('input', { bubbles: true }));
    };

    // Handle media insertion event from server
    window.addEventListener("phx:insert-media", (e) => {
      if (e.detail && e.detail.items) {
        e.detail.items.forEach(function(item) {
          if (window.postsEditorInsertMedia) {
            window.postsEditorInsertMedia(item.url, item.type);
          }
        });
      }
    });
  </script>

